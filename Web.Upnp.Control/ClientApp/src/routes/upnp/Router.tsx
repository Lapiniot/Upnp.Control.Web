import React from "react";
import { RouteComponentProps } from "react-router";
import DeviceRouter from "../common/DeviceRouter";

export default (props: RouteComponentProps) => <DeviceRouter {...props} />